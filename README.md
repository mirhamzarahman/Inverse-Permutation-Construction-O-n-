# Inverse-Permutation-Construction-O-n-
This solution reconstructs the inverse permutation by reversing the gift mapping between friends. For every friend who gives a present, the recipient's position is updated with the giver's index. A single traversal builds the answer in linear time using an auxiliary array, providing an efficient O(n) solution.
